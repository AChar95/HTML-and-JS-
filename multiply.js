function multiplyOne(a, b) {
    let valueOne = a * b;
    return valueOne;
}


function multiply(a, b) {
    let value = a * b;
    function printOut(number) {
        console.log(value);
    }
    return value;
}
const multi = (a, b) => a * b;

multi(5, 6);